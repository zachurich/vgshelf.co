import React from "react";

const Title = ({ header }) => {
  return (
    <div className="container text-center mx-auto px-6 py-10">
      <h1>{header}</h1>
    </div>
  );
};

export default Title;
