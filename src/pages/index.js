import React from "react";
import { Meta } from "../components/index";

import "../styles/index.css";

const Home = ({ user }) => {
  return (
    <div>
      <Meta title={"Home"} />
    </div>
  );
};

export default Home;
